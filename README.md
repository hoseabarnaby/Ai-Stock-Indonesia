# AstraQuant CryptoFX v40

Fix chart blank/half-black and lock chart gestures.

- Chart uses local locked canvas renderer with Yahoo/CoinGecko market data.
- Falls back to saved/live asset data so chart still appears when chart providers are slow.
- No zoom, no drag, no chart scroll capture.
- Mobile menu fixes retained.
- Supabase schema unchanged.
